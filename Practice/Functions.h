#include <vector>
#include <iostream>
using namespace std;
#include "Vehicle.h"
#pragma once

void addMotorcycle(vector<Vehicle *> &vehicleContainer);
void addCar(vector<Vehicle *> &vehicleContainer);